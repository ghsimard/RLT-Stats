# Database connection - Option 1: Connection string
DB_CONNECTION_STRING=postgresql://user:password@hostname:port/database

# Database connection - Option 2: Individual parameters
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cosmostats

# Server configuration
PORT=4001

# CORS settings
CORS_ORIGINS=http://localhost:4000,https://yourdomain.com

# Environment
NODE_ENV=development 